export class Guess {
  constructor(
    private readonly userName: string,
    private readonly position: number,
    private readonly value: number
  ) {}
  // TODO: convert all models to similar style
}
